apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: review-api
  labels:
    app.kubernetes.io/name: review-api
spec:
  hosts:
    - review-api.dev.platform.ohanyere.internal
  gateways:
    - istio-system/platform-gateway
  http:
    - route:
        - destination:
            host: review-api
            port:
              number: 80
          weight: 100
